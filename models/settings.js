class Settings {
    constructor(
        id,
        projectName,
        vatPercentage
    ) {
        this.id = id,
        this.projectName = projectName,
        this.vatPercentage = vatPercentage
    }
}
module.exports = Settings